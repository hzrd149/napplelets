{
  "name": "napplelets",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A monorepo of small NIP-5D napplets built on the @napplet packages.",
  "scripts": {
    "new": "node scripts/new-napplet.mjs",
    "dev": "node scripts/dev.mjs",
    "prepare": "pnpm build",
    "build": "turbo run build --filter=...{./napplets/*}",
    "verify": "pnpm test && turbo run type-check build --filter={./napplets/*}",
    "test": "pnpm test:adopt && turbo run test --filter={./napplets/*}",
    "test:adopt": "node --test scripts/lib/adopt.test.mjs",
    "type-check": "turbo run type-check --filter={./napplets/*}",
    "discover": "napplet discover --all",
    "test:conformance": "pnpm build && napplet conformance --all",
    "login": "napplet keys store --name publisher --prompt-sec && napplet keys use --name publisher",
    "logout": "napplet keys delete --name publisher",
    "keys": "napplet keys",
    "deploy": "pnpm build && napplet deploy --all",
    "deploy:dry": "pnpm build && napplet deploy --all --dry-run",
    "debug": "napplet debug --all",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@11.10.0",
  "devDependencies": {
    "@kehto/cli": "^0.2.16",
    "@napplelets/napplet-cli": "workspace:*",
    "@napplet/conformance-cli": "^0.2.15",
    "prettier": "^3.9.5",
    "prettier-plugin-svelte": "^3.5.2",
    "turbo": "^2.10.5"
  },
  "license": "MIT"
}
