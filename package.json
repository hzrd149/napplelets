{
  "name": "napplelets",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A monorepo of small NIP-5D napplets built on the @napplet packages.",
  "scripts": {
    "new": "node scripts/new-napplet.mjs",
    "dev": "node scripts/dev.mjs",
    "prepare": "pnpm build",
    "build": "turbo run build --filter=...{./napplets/*} --filter=@napplet/conformance-cli",
    "verify": "turbo run type-check build --filter={./napplets/*}",
    "type-check": "turbo run type-check --filter={./napplets/*}",
    "discover": "napplet discover --all",
    "test:conformance": "pnpm build && napplet conformance --all",
    "deploy": "pnpm build && napplet deploy --all",
    "debug": "napplet debug --all",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.8.0",
  "devDependencies": {
    "@kehto/cli": "^0.2.8",
    "@napplelets/napplet-cli": "workspace:*",
    "prettier": "^3.4.2",
    "prettier-plugin-svelte": "^3.3.2",
    "turbo": "^2.9.14"
  },
  "license": "MIT"
}
